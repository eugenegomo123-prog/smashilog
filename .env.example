# Copy this file to `.env` and fill in your real connection string.
# This is separate from `.dev.vars`: drizzle-kit runs under plain Node (not the Workers
# runtime) and only reads `.env` / `process.env`, so it needs its own copy of this value.

DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
